{
  "name": "payd-root",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names stellar,vite -c gray,green --pad-prefix \"stellar scaffold watch --build-clients\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "format": "npm run format --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}